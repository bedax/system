#!/bin/sh

wget \
    --mirror \
    --wait=2 \
    --page-requisites \
    --execute robots=off \
    --no-parent \
    --convert-links \
    --adjust-extension \
    --restrict-file-names=windows \
    "$1"
