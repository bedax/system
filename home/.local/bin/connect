#!/bin/sh

notify_success() {
   notify-confirmation "connected"
}

notify_failure() {
   notify-confirmation-critical "unable to connect"
}

(gksudo service networking restart && notify_success) || notify_failure
